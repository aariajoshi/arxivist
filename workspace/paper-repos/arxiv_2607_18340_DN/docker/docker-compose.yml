version: "3.8"

services:
  benchmark:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    volumes:
      - ../configs:/workspace/configs
      - ../results:/workspace/results
    command: ["python", "run_benchmark.py", "--config", "configs/config.yaml"]

  notebook:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8888:8888"
    volumes:
      - ../notebooks:/workspace/notebooks
      - ../results:/workspace/results
    command:
      [
        "jupyter", "notebook",
        "--ip=0.0.0.0", "--port=8888", "--no-browser", "--allow-root",
        "--NotebookApp.token=''"
      ]
